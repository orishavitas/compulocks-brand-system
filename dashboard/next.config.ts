import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Dashboard is a standalone Next.js app within the monorepo.
  // It reads sync state from sync-state/ in the parent repo root.
};

export default nextConfig;
